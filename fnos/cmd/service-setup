#!/bin/bash

LOG_FILE="${TRIM_PKGVAR}/${TRIM_APPNAME}.log"
PID_FILE="${TRIM_PKGVAR}/${TRIM_APPNAME}.pid"

APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR="${TRIM_PKGVAR}"
LISTEN_ADDR=":8011"
GATEWAY_PREFIX="/app/fnos-apps-store"
GATEWAY_SOCKET="${APP_DIR}/fnos-apps-store.sock"
export LISTEN_ADDR GATEWAY_PREFIX GATEWAY_SOCKET

SERVICE_COMMAND="$APP_DIR/store-server"
SVC_BACKGROUND=y
SVC_WRITE_PID=y
